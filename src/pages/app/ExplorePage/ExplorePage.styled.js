import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const Page = styled(Box)(({ theme }) => ({
    margin: `${theme.spacing(11)} ${theme.spacing(14)} ${theme.spacing(11)} ${theme.spacing(15)}`,
}));